<div style="jumbotron">
  <h2>What should I put here?</h2>
</div>



  <span style="float:left">
    <div class="jumbotron">
      <small><h3>How Much Will it Cost?</h3><small>Use This tool to Estimate the Cost of your Adventure Before Checkout</small><br><br>
      <%= simple_form_for @calculator do |f| %>
         Number of People @ (600 ft.)- $65.00 per person <%= f.input_field :six_hundred, collection: 0..15, default: 0, :input_html => {:style => 'width: 25%'}%><br><br>
         Number of People @ (800 ft.)- $75.00 per person <%= f.input_field :eight_hundred, collection: 0..15, default: 0, :input_html => {:style => 'width: 25%'}%><br><br>
         Number of Photo Packages- $30 each <%= f.input_field :photo_package,collection: 0..5, default: 0, :input_html => {:style => 'width: 25%'}%><br><br>
         Discount Code <%= f.input_field :discount_code, placeholder: "Only 1 code allowed", :input_html => {:style => 'width: 100%'} %><br><br>
        <%= f.button :submit, "Estimate Total" %>
        </small>
      <% end %>
    </div>
  </span>

  <span style="float:right; padding-right:10px">
    <div class="jumbotron">
      <h3 style="color:orange">Upcoming Dates</h3>
      <p style="font-size: 11px">To check openings for an upcoming date, click on that date below.<br>
      If your desired date is not listed, use the Date Selector at the top of the screen.</p>

      <% now =  Time.at(Time.now.utc + Time.zone_offset('PST')) %>
      <% formatted_now = now.strftime("%m-%d-%Y").to_s %>
      <% two_weeks = (now + 14.days) %>
      <% formatted_two_weeks = two_weeks.strftime("%m-%d-%Y").to_s %>

      <% @playdays.each do |playday| %>
        <% if playday.date < formatted_two_weeks %>
          <% date = (Date.strptime(playday.date, '%m-%d-%Y')).strftime("%A, %b %d") %>
          <%= link_to "#{date}", playday_path(playday) %><br>
        <% end %>
      <% end %>
    </div>
